String Concatenation in Python

This simple Python program demonstrates how to concatenate (join) two strings using the + operator.

📌 Code Example
# concatenation of two strings
a = 'hello'
b = 'world'
result = a + ' ' + b
print('concatenation', result)

✅ Explanation

Two string variables are created:
a = 'hello'
b = 'world'

The + operator is used to join them with a space (' ') in between.

The final output will be:

concatenation hello world

🎯 What You Learn

How to store strings in variables

How to concatenate strings

How to print combined output

▶️ How to Run

Save the code in a file named concat.py

Run it using:

python concat.py


You will see the concatenated result printed on the screen.
